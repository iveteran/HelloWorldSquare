1) lsb_release -a
2) apt search [-n|--names-only] <TERM>  # search names only, not a partial term in every field

3) show screen resolution
$ xrandr

4) Reload a Linux user's group assignments without logging out
  su - $USER

5) qmv - batch rename files
6) rename.ul -- FROM_STR TO_STR *.png  # debain 12

7) iostat -d -m
   watch -n 1 'iostat -d -m'
